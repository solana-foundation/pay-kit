# Codex Round 5 — Swift x402 exact client (pay-kit#129)

Rebased onto `fork/pr/x402-harness-intent` (which itself sits on
`fork/pr/transversal-cleanup`). Tip: see PR head after push.

## Summary

- **Real P1 findings**: 0
- **Confidence**: 4/5
- **Scope delta vs r4**: rebase + cross-spine wiring only; no new logic
  in `swift/Sources/X402/`.
- **Tests**: 20/20 XCTest cases pass on darwin (`swift test --filter X402Tests`).
- **Build**: `swift build` clean on darwin (Swift 6 toolchain, macOS 13+).

## Rebase delta

The rebase replayed `feat(swift): port x402 exact client from x402-sdk #26`
on top of the harness-intent base. The single conflict was in
`tests/interop/src/implementations.ts`, where the parent branch had
already added `ts-x402` and `rust-x402` client entries. The merge keeps
both upstream entries and appends the Swift entry, preserving:

- `intents: ["x402-exact"]` filter so the Swift client only joins the
  x402 matrix and never leaks into the legacy charge matrix.
- `process.platform === "darwin"` runtime gate so Linux CI runs that
  list `swift-x402-client` in `MPP_INTEROP_CLIENTS` no-op instead of
  failing during `swift run` bootstrap.

No Swift source files changed during rebase; the X402, X402InteropClient,
and X402Tests targets are byte-identical to r4.

## Cross-spine wiring review

- `Package.swift` executable product `x402-interop-client` matches the
  command tuple in `implementations.ts` (`swift run --package-path
  ../../swift x402-interop-client`). Verified by `swift build`.
- `X402InteropClient/main.swift` reads the canonical env vars used by
  every other adapter (`X402_INTEROP_TARGET_URL`, `X402_INTEROP_RPC_URL`,
  `X402_INTEROP_NETWORK`, `X402_INTEROP_PREFER_CURRENCIES`,
  `X402_INTEROP_CLIENT_SECRET_KEY`). No drift vs the TS/Rust client
  fixtures.
- Output JSON shape (`type`, `implementation`, `role`, `ok`, `status`,
  `responseHeaders`, `responseBody`, `settlement`) matches the harness
  expectation, including explicit `NSNull` for absent fixture settlement
  headers (parity with TS adapter).
- `intents: ["x402-exact"]` correctly limits the client to scenarios
  whose intent matches, satisfying the harness filter at
  `tests/interop/test/x402-exact.e2e.test.ts:46-49`.

## Local matrix run (darwin)

```
X402_INTEROP_MATRIX=1 \
MPP_INTEROP_CLIENTS=swift-x402-client \
MPP_INTEROP_SERVERS=ts-x402,rust-x402 \
INTENT=x402-exact \
pnpm exec vitest run test/x402-exact.e2e.test.ts
```

Tests skip cleanly when live testnet env vars (`X402_INTEROP_RPC_URL`,
`X402_INTEROP_MINT`, `X402_INTEROP_PAY_TO`, `*_SECRET_KEY`) are absent —
which is the documented contributor default. Full cross-spine evidence
runs on devnet with credentials; the 90/90 upstream evidence from x402
PR #26 still stands for `client × {ts-server, rust-server}`.

## CI placement (unchanged from r4)

`enabled: isEnabled(...) && process.platform === "darwin"` keeps the
ubuntu-latest interop job a no-op for the Swift client. macOS jobs that
opt in via `MPP_INTEROP_CLIENTS=swift-x402-client` exercise the matrix.

## Open items

None blocking. POST/body-replay paths remain explicitly out of scope
per the comment in `main.swift:26-33`; this matches the harness which
only exercises GET-protected routes.
