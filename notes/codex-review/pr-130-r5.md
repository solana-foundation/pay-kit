# Codex Review — Lua x402 exact server (Round 5)

Source: pay-kit PR #130 rebased onto #131 (`x402-harness-intent`) tip
`567fdaf`.

## Result
- **Real P1 findings:** 0
- **Confidence:** 4/5
- **Cross-spine wiring:** lua-x402-server opt-in registered with
  `intent: "x402-exact"`; ts-x402-client and rust-x402-client iterate
  against it under `X402_INTEROP_MATRIX=1`.
- **Matrix pairs surfaced:** 2 (ts-x402 → lua-x402-server,
  rust-x402 → lua-x402-server). Both pair entries are emitted by the
  harness; full end-to-end execution is gated by the upstream
  `allowedPair` predicate in #131 (not touched in this PR per scope).
- **MPP §19.6:** cross-server portability + idempotent-resubmit clean
  (carried from Round 4; no adapter logic changes in r5).

## Delta vs. Round 4
- Rebased single commit `feat(lua): port x402 exact server from
  x402-sdk #21` onto `fork/pr/x402-harness-intent`.
- Resolved `tests/interop/src/implementations.ts` conflict: preserves
  the upstream `ts-x402` and `rust-x402` server entries and appends
  `lua-x402-server` with `intents: ["x402-exact"]` so the harness
  filter picks it up.
- No changes to `lua/x402/bin/interop-server.lua` or
  `lua/x402-dev-1.rockspec` — both files are identical to r4.

## Hardening carried over from r4
- luasec TLS peer verification (peer + TLSv1.2+; SSLv2/v3/TLSv1.0/1.1
  disabled).
- Pure-Lua 256-bit modular field over the Ed25519 prime with
  independent ATA PDA re-derivation.
- Fee-payer instruction-account sweep (only legitimate ATA-create
  payer carve-out).
- HTTP server fee-payer keypair loaded at startup with typed-error
  exits.
- Lighthouse instruction passthrough (spine-parity with sibling
  adapters).
- Token program validated against `extra.tokenProgram`.

## Scope confirmation
Server-only for the M2 milestone. Single-file adapter at
`lua/x402/bin/interop-server.lua` plus the `x402-dev-1` rockspec,
wired into the cross-language interop harness as `lua-x402-server`
(opt-in via `MPP_INTEROP_SERVERS=lua-x402-server`,
`INTENT=x402-exact`).

## Cross-spine matrix evidence
Command:
```
MPP_INTEROP_CLIENTS=ts-x402-client,rust-x402-client \
MPP_INTEROP_SERVERS=lua-x402-server \
INTENT=x402-exact \
pnpm exec vitest run test/x402-exact.e2e.test.ts \
  --testTimeout 240000 --hookTimeout 240000
```
Vitest enumerates the cross-spine pair set; the ts→lua and rust→lua
entries are surfaced as the two new harness rows. Full settlement
requires `X402_INTEROP_MATRIX=1` plus the documented env contract
(`X402_INTEROP_RPC_URL`, mint, pay-to, funded keypairs); pair
execution semantics are owned by #131's `allowedPair` predicate.
